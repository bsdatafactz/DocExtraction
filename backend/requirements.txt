fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
python-multipart==0.0.12
pymupdf==1.24.11
python-dotenv==1.0.1
httpx==0.27.2
openai==1.51.0
bcrypt==4.2.0
pyjwt==2.9.0
email-validator==2.2.0
pytest==8.3.3

# OCR path for scanned/image-based documents (app/services/parsing.py).
# Pinned to versions actually verified against this codebase (2.x-era
# pins didn't match what pip resolves for Python 3.12 — paddleocr 3.x is a
# different, heavier API than 2.x). parsing.py runs with enable_mkldnn=False
# — this CPU's oneDNN build hit an unimplemented PIR-attribute conversion in
# PaddlePaddle's inference backend; plain CPU kernels work but are noticeably
# slower (~50-60s/page in this environment). If your deployment target has a
# working MKLDNN build, removing that flag should speed OCR up substantially.
paddlepaddle==3.3.1
paddleocr==3.7.0
